/// <reference types="react" />
import { InterfaceButtonProps } from "./Button.types";
declare const Button: ({ type, onClick, disabled, theme, height, width, round, alt, text, iconLeft, iconRight, classNames, ...rest }: InterfaceButtonProps) => JSX.Element;
export default Button;
